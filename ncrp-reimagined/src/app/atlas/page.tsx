import Link from "next/link";
import { ArrowRight, FileWarning } from "lucide-react";
import { PATTERNS } from "@/data/patterns";
import SiteHeader from "@/components/SiteHeader";

export default function AtlasPage() {
  return <div className="min-h-[100dvh] bg-paper"><SiteHeader current="atlas" /><main id="main-content" className="public-shell py-8 sm:py-12"><div className="mx-auto max-w-5xl"><div className="border-b border-line pb-7"><div className="flex items-start gap-3"><FileWarning size={23} className="mt-1 shrink-0 text-danger" aria-hidden="true" /><div><p className="kicker text-danger">Threat bulletin / pattern corpus</p><h1 className="mt-3 text-3xl font-bold tracking-[-.04em] text-ink sm:text-4xl">Active scam scripts in the atlas.</h1><p className="mt-4 max-w-2xl text-base leading-7 text-ink-soft">These are behavioural playbooks sourced from public advisories and represented with anonymised examples. A pattern match is a warning, not a finding of guilt.</p></div></div></div><div className="mt-8 overflow-hidden border-y border-line bg-surface">{PATTERNS.map((pattern, index) => <Link key={pattern.slug} href={`/atlas/${pattern.slug}`} className="group grid gap-4 border-b border-line p-5 last:border-b-0 sm:grid-cols-[70px_1fr_auto] sm:items-center sm:gap-7"><span className="font-mono text-xs font-bold text-ink-faint">0{index + 1}</span><span><span className="flex flex-wrap items-center gap-2"><span className="rounded-[5px] bg-danger-soft px-2 py-1 font-mono text-[10px] font-bold uppercase tracking-[.1em] text-danger">High risk</span><span className="text-xs text-ink-faint">{pattern.channels.slice(0, 3).join(" / ")}</span></span><span className="mt-2 block text-lg font-bold text-ink group-hover:text-service">{pattern.name}</span><span className="mt-1 block text-xs text-ink-faint">{pattern.aliases.slice(0, 2).join(" · ")}</span><span className="mt-3 block text-sm leading-6 text-ink-soft">{pattern.stages[0]?.signals[0]}</span></span><span className="inline-flex min-h-11 items-center gap-2 text-sm font-bold text-service">Read playbook <ArrowRight size={16} aria-hidden="true" /></span></Link>)}</div><p className="mt-7 max-w-2xl text-xs leading-5 text-ink-faint">Sources and retrieval dates are shown on each pattern page. Verify primary sources before using any information in a real complaint.</p></div></main></div>;
}
